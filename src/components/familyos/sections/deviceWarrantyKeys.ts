// Split out so other modules (Hermes, dashboard alerts) can reference this key
// without statically pulling in the whole (lazy-loaded) component.
export const DEVICES_STORAGE_KEY = 'familyos_devices';
